// const inputField = document.querySelector('.massage-input');

// inputField.addEventListener('input', () => {
//   inputField.style.height = 'auto'; // إعادة تعيين الارتفاع
//   inputField.style.height = `${inputField.scrollHeight}px`; // ضبط الارتفاع بناءً على المحتوى
// });
